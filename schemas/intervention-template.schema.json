{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://choice-gap.local/schemas/intervention-template.schema.json",
  "title": "InterventionTemplate",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "version",
    "name",
    "family",
    "targetMechanism",
    "applicableTargets",
    "interactionCost",
    "difficulty",
    "instructionTemplate",
    "outcomeDefinition",
    "minimumPracticeCount",
    "progressionCriteria",
    "evidenceStatus",
    "active"
  ],
  "properties": {
    "id": { "type": "string", "pattern": "^[a-z0-9_]+$" },
    "version": { "type": "integer", "minimum": 1 },
    "name": { "type": "string", "minLength": 1 },
    "family": {
      "enum": ["awareness", "reflection", "pause", "if_then", "intentional_choice", "stop_rule", "alternative", "environment"]
    },
    "targetMechanism": {
      "enum": ["awareness", "trigger_awareness", "urge_control", "intentional_choice", "stopping_control", "alternative_available"]
    },
    "applicableTargets": {
      "type": "array",
      "minItems": 1,
      "uniqueItems": true,
      "items": { "const": "youtube" }
    },
    "interactionCost": { "enum": ["low", "medium", "high"] },
    "difficulty": { "type": "integer", "minimum": 1, "maximum": 5 },
    "preconditions": { "type": "array", "items": { "type": "string" }, "default": [] },
    "safetyConstraints": { "type": "array", "items": { "type": "string" }, "default": [] },
    "instructionTemplate": { "type": "string", "minLength": 1 },
    "outcomeDefinition": { "type": "string", "minLength": 1 },
    "minimumPracticeCount": { "type": "integer", "minimum": 1 },
    "progressionCriteria": {
      "type": "array",
      "minItems": 1,
      "items": { "type": "string", "minLength": 1 }
    },
    "evidenceStatus": { "enum": ["conversation_hypothesis", "review_pending", "reviewed"] },
    "active": { "type": "boolean" }
  }
}
